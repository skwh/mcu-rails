<%= form_for(@fireteam) do |f| %>
  <% if @fireteam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fireteam.errors.count, "error") %> prohibited this fireteam from being saved:</h2>

      <ul>
      <% @fireteam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :insignia_url %><br>
    <%= f.text_field :insignia_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
