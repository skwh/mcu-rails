<% if !current_page?('/forums/') and !current_page?(controller:'/events') %>
	<div id="event_box" class="box small-box">
		<h3><%= link_to "Events", main_app.events_path, {class:'nolink'} %></h3>
		<%= render 'events/feed' %>
	</div>
<% end %>